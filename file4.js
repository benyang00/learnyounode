var fs = require('fs');

var bufferStr = fs.readFile(process.argv[2], utf8, callback);

function callback (err, data) {
  
}

// var str = bufferObj.toString().split('\n');
//
// console.log(str.length - 1);
